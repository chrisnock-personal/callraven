; Narrow, fixed RTP range so Asterisk's brief pre-direct_media RTP allocation
; can't collide with the two callraven containers' RTP_PORT_LOW/RTP_PORT_HIGH
; ranges, since all three containers share the CI runner's host network.
[general]
rtpstart=19500
rtpend=19999
