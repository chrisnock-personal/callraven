; Enables Asterisk's built-in HTTP server, required for the PJSIP WebSocket
; transport (res_pjsip_transport_websocket) below — WS is a JsSIP-over-WS
; upgrade over this same HTTP listener, not a separate service.
[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
